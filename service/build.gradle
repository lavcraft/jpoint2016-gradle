apply plugin: 'groovy'task dist(type: Tar) {  compression 'gzip'  from fileTree(dir: file('src/docs/'), include: '**/*.adoc')}dependencies {  compile gradleApi()}artifacts {  documentationConfiguration dist}